export interface UserCardProps {
  avatar: string
  name: string
  money: number
  gamesCount: number
}

export const UserCard = ({
  avatar,
  name,
  money,
  gamesCount
}: UserCardProps) => {
  return <div></div>
}
